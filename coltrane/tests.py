import unittest
from coltrane models import Category

class CategoryTestCase(unittest.TestCase):
    """Grab the Category and see what the absolute URL is"""
    def __init__(self, arg):
        super(CategoryTestCase, self).__init__()
        self.arg = arg
        
